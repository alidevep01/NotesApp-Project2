<%- include("./partials/header.ejs") %>
<div class="mainContainer">
  <!-- <ul class="notesul">
    <div class="list-container">
      <li class="list"></li>
      <li class="list"><%= showNote.description %></li>
      <li class="list"><%= showNote.img %></li>
    </div>
  </ul> -->
  <div style="background-color: <%=showNote.color%>" class="card text-center">
    <!-- -------------------- Note Name -------------------- -->
    <div class="card-header"><%= showNote.name %></div>
    <div class="card-body">
      <!-- -------------------- Title -------------------- -->
      <h5 class="card-title"><%= showNote.title %></h5>
      <!-- -------------------- Description-------------------- -->
      <p class="card-text"><%= showNote.description %></p>
      <!-- -------------------- image-------------------- -->
      <img src="<%= showNote.img%> " alt="Image" />
    </div>
  </div>
  <%- include('./partials/footer.ejs') %>
</div>
